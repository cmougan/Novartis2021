from setuptools import setup

setup(
   name='metrics',
   version='1.0',
   description='A useful module',
   author='Man Foo',
   author_email='foomail@foo.com',
   packages=['metrics'],  #same as name
)